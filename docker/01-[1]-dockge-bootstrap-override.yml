# 01-[1]-dockge-bootstrap-override.yml
# Temporary bootstrap override used before Traefik/AuthentiK access is ready.
# Script 7 redeploys Dockge without this override to close direct access.

services:
  dockge:
    ports:
      - "${ADMIN_UI_BOOTSTRAP_BIND:-0.0.0.0}:${DOCKGE_BOOTSTRAP_PORT:-5001}:5001"
